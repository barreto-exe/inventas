<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teamihc.inventas">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icono"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InVentas">
        <activity android:name="com.teamihc.inventas.HistorialDeCambios"></activity>
        <activity android:name="com.teamihc.inventas.AggArticuloClass" />
        <activity
            android:name="com.teamihc.inventas.VenderClass"
            android:parentActivityName="com.teamihc.inventas.VenderClass" />
        <activity
            android:name="com.teamihc.inventas.CrearProductoClass"
            android:parentActivityName="com.teamihc.inventas.CrearProductoClass" />
        <activity android:name="com.teamihc.inventas.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>